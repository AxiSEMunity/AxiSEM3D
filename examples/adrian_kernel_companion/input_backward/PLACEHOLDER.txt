# NOT NEEDED – the backward (adjoint) simulation is generated automatically.
#
# Phase 2 note: this directory is intentionally left unused.  The adjoint
# input files are created at runtime by axikernels
# (XObjectiveFunction.compute_backward_field), which copies and modifies the
# forward input directory.  There is no need to manually populate this folder.
#
# If you want to inspect the generated backward inputs after running
# compute_kernels.py, look inside:
#   simu_forward/output/elements/../../  (parent of the forward simulation dir)
#   backward_<forward-sim-name>/input/
#   inparam.source.yaml  (adjoint source definition)
#   prem_iso_elastic_40s.e
